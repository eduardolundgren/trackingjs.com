{namespace color_fish_tank}

/**
 * This template will be rendered by SoyWeb when the user loads static.soy.
 * It deliberately includes dummy data so the designer can get a feel for how
 * the task list will appear with real data rather with minimal copy and paste.
 */
{template .soyweb}
{call example.page}
  {param pageName: 'Random Particles' /}
  {param baseDir: '../' /}
  {param url: '../../bower_components/tracking.js/examples/color_fish_tank.html' /}
  {param sourceUrl: 'https://github.com/eduardolundgren/tracking.js/blob/master/examples/color_fish_tank.html' /}
  {param cssScope: 'color_fish_tank' /}
{/call}
{/template}
