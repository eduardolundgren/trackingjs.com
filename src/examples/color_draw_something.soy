{namespace color_draw_something}

/**
 * This template will be rendered by SoyWeb when the user loads static.soy.
 * It deliberately includes dummy data so the designer can get a feel for how
 * the task list will appear with real data rather with minimal copy and paste.
 */
{template .soyweb}
{call example.page}
  {param pageName: 'Draw Something' /}
  {param baseDir: '../' /}
  {param url: '../../bower_components/tracking.js/examples/color_draw_something.html' /}
  {param sourceUrl: 'https://github.com/eduardolundgren/tracking.js/blob/master/examples/color_draw_something.html' /}
  {param cssScope: 'color_draw_something' /}
{/call}
{/template}
